import { ReplaceLongStringsPipe } from './replace-long-strings.pipe';

describe('ReplaceLongStringsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLongStringsPipe();
    expect(pipe).toBeTruthy();
  });
});
